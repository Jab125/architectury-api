{
  "schemaVersion": 1,
  "id": "architectury_misc",
  "version": "${version}",
  "name": "Architectury (misc)",
  "description": "",
  "authors": [
    "shedaniel"
  ],
  "contact": {
    "issues": "a",
    "sources": "b",
    "homepage": "c"
  },
  "license": "License",
  "environment": "*",
  "mixins": [
      "architectury.mixins.json",
      "architectury-common.mixins.json"
  ],
  "entrypoints": {},
  "icon": "icon.png",
  "depends": {
      "fabricloader": ">=0.15",
    "architectury_registry": "${version}",
    "architectury_networking": "${version}",
    "architectury_fluids": "${version}",
    "architectury_extensions": "${version}",
    "architectury_hooks": "${version}",
    "architectury_events": "${version}",
    "architectury_core": "${version}"
  },
  "breaks": {}
}